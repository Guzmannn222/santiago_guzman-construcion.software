/**
 * Persistence layer. Each repository exposes CRUD/query operations over a {@code model} entity
 * (e.g. UserRepository, InventoryRepository, OrderRepository).
 * See SDD 28-29 (layered architecture) and 31 (suggested implementation order).
 */
package com.nexusmarket.repository;
